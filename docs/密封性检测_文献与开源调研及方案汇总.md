# 电机接线盒密封性检测：文献、开源与路线重构

> 版本定位：实验室可分性优先版  
> 当前目标：不是先做一个“全局统一的密封/非密封分类器”，而是先证明哪些实验室工况里确实存在可重复利用的规律，再决定模型复杂度与现场迁移方式。

---

## 1. 问题重述

### 1.1 当前真实难点

这项工作当前卡住的根因，不是“模型还不够大”，而是下面这几个事实同时成立：

- 密封与非密封在**多数滑动窗口**上并没有稳定区分度。
- 仅有四路输入：`Tin`、`Tout`、`Hin`、`Hout` 与时间戳，观测维度有限。
- 影响因素耦合严重：外部湿度、热源开停、材料吸放湿、呼吸效应、状态切换时滞。
- 当前最可信的标签粒度更接近**文件级或状态段级**，不是密集窗口级标签。
- 实验室与现场存在明显分布迁移，实验室学到的“标准模式”不能直接假定可迁移。

### 1.2 因此需要修正的问题定义

近阶段问题应改写为：

1. 哪些工况本身就是低信息工况，应该直接排除出异常训练集？
2. 哪些工况或状态转移段存在稳定的可分性证据？
3. 在这些高信息工况内，哪类模型分支最有效？
4. 当且仅当多个分支存在互补性时，才讨论融合层，最后才讨论 MoE 或复杂集成。

### 1.3 近阶段不做什么

- 不以“实验室大部分工况统一建模成功”为近期目标。
- 不把现场迁移作为这一步的成败标准。
- 不因为单一分支表现不稳，就直接跳到“更大的模型”或“全局 MoE”。

---

## 2. 已有负结果证据

以下结论来自当前阶段汇报中已经展示过的实验现象，说明“全局统一分类边界”并不存在，或者至少当前还没有足够证据支持它存在。

### 2.1 低信息工况已经被反复验证

- **外部环境稳定 - 内部无热源**：密封与不密封整体变化趋势接近，差异不显著。
- **外部环境稳定 - 内部有热源**：升温后内部相对湿度会出现短时尖峰，但密封与不密封整体仍难区分。
- **内部积湿 - 有/无热源**：曲线可能持续高湿，但切换后未必出现清晰突变，难直接作为密封判别依据。

这些结果说明：

- 部分工况的主要价值不是“做分类”，而是帮助我们识别**哪些窗口不该送进异常模型**。
- 如果不先排除这些窗口，模型很容易学到伪相关或背景噪声。

### 2.2 目前看到的高价值线索是“特定工况/特定阶段”

- **外部高湿驱动 - 内部无热源**：不密封后内部湿度缓慢抬升，这是一类有物理解释的候选有效信号。
- **外部高湿背景 + 热源停止后的冷却段**：降温收缩形成负压，外部高湿空气进入内部，属于状态变化更明显的阶段。
- **中途打孔 / 密封转非密封**：这是最适合验证“状态转移附近分数是否持续抬升”的场景。

### 2.3 当前应接受的结论

当前最重要的结论不是“模型失败了”，而是：

- **多数窗口不可分** 是已知事实，不是训练技巧问题。
- 近阶段必须先回答“哪里可分”，再回答“怎么建模”。
- “低信息工况排除 + 高信息工况突破 + 复杂耦合工况延后”才是合理路线。

---

## 3. 工况分层与目标层级

## 3.1 三类工况角色

| 工况角色 | 当前代表场景 | 当前定位 | 近期处理策略 |
|---|---|---|---|
| 低信息工况 | 外部环境稳定-无热源；外部环境稳定-有热源 | 显式排除集 | 仅标记或 gate 掉，不进入异常训练 |
| 高信息工况 | 外部高湿-无热源；冷却降温段；中途打孔转移段 | 首批主战场 | 先证明可分性，再做分支模型 |
| 复杂耦合工况 | 外部波动明显、热湿多因素叠加 | 后续难点集 | 暂不作为首轮成败标准，后续再做深度模型验证 |

## 3.2 任务层级固定为四层

1. **信息窗口筛选**  
   先排掉明显低信息窗口，保留可能有物理信息的候选窗口。

2. **工况识别 / 粗分层**  
   按窗口所属工况族或状态段做条件化分析，而不是全局混合建模。

3. **工况内风险建模**  
   只在高信息工况内比较判别分支、残差分支、相似性分支。

4. **事件级融合判定**  
   最后再做迟滞、持续时间、冷却和事件级输出；融合层默认从轻量模型开始。

## 3.3 当前成功标准

当前“做出来”的定义不是覆盖所有工况，而是满足下面两点：

- 在**少数高信息工况**下，能稳定区分或稳定排序出非密封风险。
- 在**状态转移场景**下，异常分数能在转移邻域附近持续抬升，而不是只出现孤立尖峰。

---

## 4. 文献与开源地图

## 4.1 检索顺序必须固定

先找三类材料，再找更复杂模型：

1. 为什么多数窗口不可分  
2. 怎样筛信息窗口、怎样做工况/阶段分层  
3. 怎样做事件级评估与可靠验证  
4. 最后才是更强模型或更复杂融合

这个顺序不能反过来。

## 4.2 窗口筛选 / 分段 / 特征抽取

1. [ruptures](https://github.com/deepcharles/ruptures)  
   用途：做变化点检测、状态切换定位、阶段切分。  
   对当前问题的价值：比“整段统一滑窗”更适合处理中途打孔、热源开停、冷却段切换。

2. [STUMPY / Matrix Profile](https://stumpy.readthedocs.io/en/latest/Tutorial_The_Matrix_Profile.html)  
   用途：做 motif/discord 搜索、异常片段和相似窗口检索。  
   对当前问题的价值：适合做“健康窗口库”与“相似性分支”的基础设施，而不是先上复杂深度检索模型。

3. [tsfresh](https://tsfresh.readthedocs.io/en/main/)  
   用途：系统化提取统计、趋势、波动、相关性等时间序列特征。  
   对当前问题的价值：用于先构建可解释的规则 + XGBoost/LightGBM 分支，而不是一开始端到端黑盒。

## 4.3 基准与评估

1. [TSB-AD](https://github.com/TheDatumOrg/TSB-AD)  
   用途：提供更严格的时序异常检测评估视角。  
   对当前问题的启发：先把评估做可靠，再谈模型优劣。

2. [TSB-AutoAD](https://github.com/TheDatumOrg/TSB-AutoAD)  
   用途：自动化时序异常检测 benchmark。  
   对当前问题的启发：自动化集成或复杂配置并不天然优于简单路线，尤其在分布迁移下更要谨慎。

3. [Merlion](https://github.com/salesforce/Merlion)  
   用途：提供统一的基线、集成与评估工具。  
   对当前问题的价值：更适合做离线横评和对照，不适合作为当前问题的直接答案。

4. [Do Deep Neural Networks Contribute to Time Series Anomaly Detection?](https://www.sciencedirect.com/science/article/pii/S0950705122010094)  
   用途：审视深度模型在时序异常任务中的真实增益。  
   对当前问题的启发：当数据定义和评估口径不稳时，盲目上更深模型通常不会先解决根因。

## 4.4 深度分支代表方法

1. [TranAD](https://github.com/imperial-qore/TranAD)  
   用途：多变量时序异常检测代表性深度基线。  
   当前角色：作为残差/深度分支的对照组，而不是默认主路线。

2. [Anomaly Transformer](https://openreview.net/forum?id=LzQQ89U1qm_)  
   用途：基于 attention 的异常关联建模。  
   当前角色：适合后续复杂耦合工况做代表性深度对照，不适合替代前面的工况筛选和窗口筛选。

## 4.5 结合当前问题后的文献结论

1. 当前应优先补的是**可分性证据链**，不是更大的模型清单。
2. 当前最有工程价值的路线是：**工况分层 + 可解释基线 + 状态转移验证**。
3. MoE、stacking、boosting 可以保留，但必须放在“至少两个分支已有互补性证据”之后。

## 4.6 当前落地状态说明

为避免“文献里提到的都已经工程接入”的误解，这里明确区分三种状态：

1. **已直接体现在当前工程脚本中的方法**
   - `XGBoost` 判别支线：已实现并完成离线验证，但当前未通过验收。
   - 相似性 / 记忆分支：已实现并完成离线验证，当前已达到支线通过标准。
   - 分支感知路由、转移段相对打分、静态阈值基线：已实现并作为当前主路线的一部分。

2. **已体现在方案思想中，但尚未直接接入对应开源库**
   - `TSB-AD / TSB-AutoAD`：主要体现在评估口径上，例如 `leave-one-run-out`、避免相邻窗口泄漏、先做可靠验证再比较模型。
   - `STUMPY / Matrix Profile`：主要体现在当前“相似性 / 记忆分支”的设计思想上，但目前仍是自实现的轻量记忆检索，不是直接调用 `STUMPY`。
   - `tsfresh`：主要体现在当前先使用可解释统计量、斜率、差分、波动等特征构建规则和判别支线，而不是直接调用 `tsfresh` 做全量特征抽取。
   - `ruptures`：主要体现在当前优先重视状态切换、阶段切分和转移邻域验证的路线设计上，但还没有正式接入 `ruptures` 做变化点检测。

3. **当前仍作为后续储备方法，尚未进入主链路**
   - `Merlion`：保留作后续离线横评和基线对照工具。
   - `TranAD`、`Anomaly Transformer`：保留作残差/深度分支的代表性对照方法，目前未进入当前主链路。

因此，第 4 节当前的作用是：**给路线提供方法论锚点和实现优先级，而不是要求所有开源项目都已经接入代码。**

---

## 5. 实验室优先路线

## 5.1 第一阶段：先证明“哪里可分”

第一阶段的核心问题只有一个：

**在实验室数据里，哪些工况族或状态转移段是可利用的？**

第一阶段固定验证三类场景：

1. **低信息对照场景**  
   - 外部环境稳定-无热源  
   - 外部环境稳定-有热源  
   预期结果：不是分类成功，而是被 gate 掉或仅作标记。

2. **高价值场景**  
   - 外部高湿-无热源  
   预期结果：内部湿度缓慢抬升成为第一类可重复利用信号。

3. **状态转移场景**  
   - 中途打孔 / 密封转非密封  
   预期结果：分数在转移邻域附近持续抬升，而不是仅出现孤立尖峰。

## 5.2 第二阶段：只做受控的三分支基线

只有在第一阶段找到至少一个高信息工况族之后，才进入三分支比较：

1. **判别分支**  
   规则 + `XGBoost/LightGBM`  
   输入来自工况内特征、差分特征、斜率、波动、阶段统计量。

2. **残差分支**  
   正常样本预测残差  
   现有 `GRU` 路线保留，但只作为一个分支。后续可加轻量 `TCN` 或代表性深度基线做对照。

3. **相似性 / 记忆分支**  
   健康窗口库 + 相似窗口检索  
   优先从 Matrix Profile 或原型库做起，不先做复杂神经记忆网络。

### 当前进展对齐

基于当前仓库内的离线验收结果，第二阶段需要按下面的事实继续推进：

1. **第一阶段当前结论是 `PARTIAL_PASS`，不是 `PASS`**  
   - 工况 gate 已通过，说明“先筛工况再分析”的路线成立。  
   - 转移分支已通过，说明“中途打孔 / 密封转非密封”场景能做事件级抬分验证。  
   - 静态高信息分支尚未通过，说明“高外湿无热源下静态 seal/unseal 稳定区分”仍未建立充分证据。

2. **判别分支已经跑过第一轮 XGBoost 对照，但当前结果不支持把它作为主推荐**  
   - `XGBoost` 在当前 `leave-one-run-out` 验证中未超过简单基线。  
   - 当前更稳的静态替代是单特征阈值基线 `delta_half_dAH`，可先作为静态分支的演示版方案。  
   - 因此当前主线不应表述为“XGBoost 已解决静态分支”，而应表述为“XGBoost 支线已完成验证，但暂未通过验收”。

3. **当前实际推荐路线应更新为**  
   `分支感知路由 -> 转移段相对打分 -> 状态段级证据融合 v3`
   - 其中转移场景已升级为 `transition_boost_alert`。
   - 静态场景当前采用 `delta_half_dAH 阈值基线 + 相似性分支 + 多视角证据增强`。
   - 静态难例当前必须允许进入 `hard_case_watch / abstain`，而不是强行输出告警或低风险。

4. **相似性分支已进入当前运行级决策层，但不等于“已经进入模型融合层”**  
   - 相似性 / 记忆分支已经完成首轮验证，并达到当前支线通过标准。  
   - 它当前作为 `evidence fuser v2` 的一条静态证据源参与运行级决策，但这仍然是规则化证据融合，不是 `Logistic Regression / LightGBM / stacking` 意义上的模型融合。  
   - 残差分支仍未完成同等强度验证，在没有新证据前，不应替代当前已验证成立的主路线。

5. **第一步当前需要从“工况 gate”升级为“分支感知路由”**  
   - 当前已完成 `gate / info selector v2` 验证，结论是：第一步优化有效，但优化重点不是继续缩窗口，而是把窗口送到正确分支。  
   - 转移邻域窗口应完整路由到转移分支；当前 `transition_near_branch_coverage = 1.0`。  
   - 静态阈值分支不应强行覆盖所有静态高信息运行，而应只在 `delta_half_dAH` 为正等 favored 窗口上工作；当前该策略在覆盖 `5` 个运行时，把 run 级 `balanced_accuracy` 从 `0.6667` 提升到 `0.8333`。  
   - 这意味着新的第一步不是单纯 “gate 掉低信息窗口”，而是要允许不同后续分支看到不同的候选窗口集合，并允许低把握度分支显式 abstain。
   - 在此基础上，当前又完成了 `gate / info selector v3` 的首轮优化验证：  
     - `transition` 分数升级为多视角版本，近转移窗口平均排序从 `0.903` 提升到 `0.942`；平均分数抬升从 `0.804` 提升到 `2.638`。  
     - `static` 分支当前没有被强行重构，仍保留 `delta_half_dAH >= 0` 的 favored 规则；`v3` 只增加诊断性的 `static_context_score_v3` 和路由理由字段。  
     - 随后又完成了 `evidence fuser v3` 的接入验证：运行级 `final_status / primary_evidence / risk_level` 与 `v2` 完全一致，没有引入回退；同时 `transition` 的 rank / lift 解释被真正带到了运行级输出层。  
     - 在此基础上又完成了 `evidence fuser v4` 的保守接入验证：只对 `外部高湿-无热源` 静态支线叠加 `no_heat probe v3`，并且仅把 `2026-03-03 181049_unseal_unheated` 这一类 `multiview support` 样例升级成更高置信的 `supported alert`；其余分支和全部 transition 运行都保持不变。  
     - 这说明当前默认路径已经可以从 `v3` 进一步升级为 `v4`，后续优化仍应继续围绕 `transition event` 的边界与展示展开，而不是重新回到全局静态分类器。

## 5.3 新补充数据的接入原则

新补充的 `new_data.zip` 不应被理解为“又多了一批可以直接混进 whole-run 训练的 seal/unseal 样本”。按当前说明表与离线分析结果，它本质上是：

- `no_change`
- `seal_change_to_unsealed`
- `heat_on / heat_off`
- `ext_humidity_up`

混合在一起的段级数据源。

因此，这批数据的正确接入方式必须是：

1. **先段级切分，再决定进入哪条主线**  
   已知改变时刻的运行必须拆成 `pre_change / post_change`，不能继续按 whole-run 直接贴一个统一标签。

2. **主战场仍然是 `外部高湿-无热源`**  
   新数据真正补强的是：  
   - 更多 `mainfield_extHigh_intLow_noHeat` 可分析段  
   - 更多 `seal->unseal` 的 transition 参考段  
   而不是“任意工况下都可以拿来补充静态分类器”。

3. **`heat_on / heat_off / ext_humidity_up` 优先进入控制挑战集**  
   这些变化首先要服务于误报控制、`watch / abstain` 和路由鲁棒性验证，而不是直接当作 anomaly 正样本。

4. **新数据优先推动“段级 baseline”而不是重启 whole-run 模型**  
   在当前阶段，更合理的是形成：  
   - `static mainfield reference segments`  
   - `transition primary runs`  
   - `control challenge segments`  
   再在这些段级对象上比较简单基线和后续分支，而不是立刻重启 whole-run `XGBoost/GRU`。

### 当前进展对齐

基于最新的新补充数据离线分析，当前已经可以把 `new_data.zip` 收口成一条与主线程一致的段级输入链：

- `segment-based pipeline v1` 已经把新数据稳定拆成：
  - `5` 个主战场干净静态参考段  
    - `2` 个 `static_negative_reference`
    - `3` 个 `static_positive_reference`
  - `3` 个 `transition_primary_mainfield` 运行
  - 一批 `control_challenge` 段（`heat_on / heat_off / ext_humidity_up / balanced / internal_moisture / breathing_watch`）
- 这说明新数据的主要贡献不是“whole-run 样本数增加”，而是第一次形成了**可控的段级样本池**。
- 随后又完成了 `segment-level static baseline v1` 的首轮验证：
  - 在 `5` 个干净静态参考段上，`apparent_coverage = 0.8`、`apparent_precision_on_resolved = 1.0`
  - `leave-one-reference-out` 下仍保持 `coverage = 0.8`、`precision_on_resolved = 1.0`
  - 但 `static_positive_eval_only` 这类弱正样本仍会落回负侧，而 `static_breathing_watch` 仍会被纯静态基线打成正样本倾向
- 在此基础上又形成了 `segment static support v1`：
  - 段级上，`static_positive_reference_support / static_negative_reference_support` 已经可以作为新数据的静态支持层
  - 运行级上，`transition_primary_support` 仍然优先于静态支持层，说明新数据接入后仍然应保持“transition 主导、static 辅助”的主线程
  - `static_review_weak_positive / static_watch_breathing / static_watch_confound` 已被显式拉入 review 队列，而不是被强行提升成主告警
- 随后又完成了扩样本后的 `multiview feature mining v1`：
  - 在排除 `duration_h` 这类实验设计投影后，当前最值得继续追的新增特征主要分三类：
    - `耦合/滞后`：`max_corr_outRH_inRH_change`、`corr_headroom_in_hum`、`best_lag_h / best_lag_level_hum / best_lag_level_ah`
    - `响应持续性`：`late_rh_gain_per_out`、`late_minus_early_rh_gain`、`positive_ah_response_ratio / headroom_gain_ratio`
    - `波动/幅度`：`amp_in_hum_p90_p10`、`std_in_hum_run`
  - 当前结果说明：  
    - `weak positive` 更像“耦合相关性对了，但累计响应和后段放大不够”  
    - `breathing_watch` 更像“若只看累计漂移会偏正，但后段持续性和 headroom 结构仍不像真正不密封”  
    - `heat-off confound` 则在很多静态漂移和响应持续性特征上都明显回到负侧
- 在此基础上又补了一轮 `multiview feature mining v2`，专门验证 `露点温度 / vapor pressure deficit / ingress proxy`：
  - 当前确认有价值的不是“直接算一个泄漏率”，而是把 `dew point`、`vpd` 和 `正驱动下的响应斜率/负响应比` 作为新的结构特征来源。
  - 其中需要明确剔除两类伪强特征：
    - 分母很小会数值发散的原始 ratio
    - `ingress_count` 这类本质上仍受时长/驱动覆盖率投影影响的计数特征
  - 如果只看干净参考段的 AUC，`dew_ingress_r2 / early_dew_gain_per_out / late_minus_early_vpd_gap / vpd_in_mean` 都会显得很强；但把 `weak_positive / breathing_watch / heat-off confound` 一起纳入 challenge-oriented 检查后，当前更像“真正能帮分难段”的反而是：
    - `max_corr_level_dew`
    - `max_corr_level_ah`
    - `max_corr_level_hum`
  - 这说明当前最值得推进的不是“把 dew/vpd 特征全量堆进统一模型”，而是把它们按用途拆成三条子评分：
    - `weak positive support`
    - `breathing suppression`
    - `confound reject`
  - 因此，新数据接入后的下一步依然应保持在主线程内部推进：先把这些新增结构特征接进 `segment static support`，而不是重启 whole-run `XGBoost / GRU`。
- 在此基础上，当前又完成了 `segment static support v2`：
  - 这一步没有改动“`transition` 主导、`static` 辅助”的主路线，只是把新增结构特征克制地收成 3 个子评分：
    - `weak_positive_support`
    - `breathing_suppression`
    - `confound_reject`
  - 当前运行级结果与预期一致：
    - `20260303-181049` 从 `static_review_weak_positive` 升级为 `static_supported_weak_positive`
    - `20260306-160246` 被进一步压实为 `static_watch_breathing_confirmed`
    - `20260321-170450 heat-off` 被进一步压实为 `static_watch_confound_confirmed`
  - 这说明新增的 `dew / ingress / level-correlation` 特征已经开始对主战场的静态解释层产生实增益，但这种增益仍然应该以 `review / watch / support` 的方式接入，而不是直接重启 whole-run 统一分类器。
  - 当前还进一步补齐了段级复核闭环：
    - `segment_review_queue_v2`
    - `segment_review_label_template_v1`
    - `segment_feedback_loop_v1`
  - 这意味着新补充数据不再只停留在离线分析层，而是已经具备“人工确认 -> 段级参考池扩容 -> 待复核段重排”的最小工程闭环。
  - 随后又完成了首轮 `post-feedback reanalysis v1`，把自动高置信回灌后的段级主战场重新收成：
    - `2 negative_reference`
    - `4 positive`（`1 supported positive + 3 transition_positive`）
    - `1 breathing_watch`
    - `1 confound`
  - 这一步带来的最重要结论不是“可以重启 whole-run 模型”，而是：
    - 扩样本后仍然站得住的不是单一静态值，而是 `早段响应 + 晚段持续性 + level-correlation` 这组结构特征；
    - 其中当前最稳的一批包括：
      - `delta_half_in_h`
      - `early_dew_gain_per_out`
      - `late_minus_early_rh_gain`
      - `late_minus_early_vpd_gap`
      - `max_corr_outRH_inRH_change`
      - `slope_in_h_per_h`
    - `181049` 现在可以保留为 `supported positive`，但它与三个 `post-change positive` 并不完全同质，后续更适合区分 `strong positive` 和 `supported positive` 两层；
    - `160246` 与 `20260321 heat-off` 继续保留为 `breathing / confound` 是正确的，当前并没有证据支持把它们抬进正参考池。
- 因此，当前最合理的解释是：
  - **新数据已经足够支撑段级静态参考池，但还不足以支撑 whole-run 统一分类器**
  - `watch / abstain / challenge` 仍然必须保留在主线程里

6. **历史旧数据已确认不适合直接并入主训练集**  
   - 旧数据中存在一批 `strict sealed` 运行，会被当前规则长时间打成 `candidate_high_info`，说明它们会对主任务形成强干扰。  
   - 当前更合理的角色是：`干扰挑战集 + 负控制集 + 动态响应特征发现集`。  
   - 因此旧数据后续应主要用于验证系统是否能 `watch / abstain`，而不是直接增加 `seal/unseal` 监督样本。
   - 在把旧数据重新投到“当前主战场已确认的结构特征空间”后，这个判断没有被推翻，只是被进一步细化：  
     - 使用当前回灌后主战场的 `8` 个核心结构特征（`delta_half_in_h`、`early_dew_gain_per_out`、`late_minus_early_rh_gain`、`late_minus_early_vpd_gap`、`max_corr_outRH_inRH_change` 等）重新投影后，`strict sealed` 里仍有 `3` 个运行会落到 `positive-like` 一侧。  
     - 这说明新增特征体系确实把“最危险的 strict sealed 污染量”从更大范围压缩到了少数硬负例，但还远不足以把旧数据重新并回主训练集。  
     - 当前更合理的利用方式应收敛为：
       - 把这 `3` 个 `strict sealed positive-like` 运行作为 `hard negative controls`
       - 把其余 `strict sealed / no_screw_grease / unsealed` 继续作为 `干扰挑战集 + 结构特征外推验证集`
       - 不把旧数据的 `unsealed` 直接当作当前主战场正参考，因为它们与当前主战场正池的一致性仍然偏弱，大多数只会落在 `ambiguous`
   - 在此基础上又补了一轮“跨数据集特征守门”检查：把当前主战场正池、当前 `breathing/confound` 难例，以及这 `3` 个旧数据 `hard negative` 放在同一特征阈值空间里做约束，当前更值得继续保留的不是所有新特征，而主要是：
     - `best_lag_level_hum`
     - `corr_headroom_in_hum`
     - `corr_out_hum_in_hum`
     - `late_rh_gain_per_out`
     - `max_corr_level_hum`
     - `best_lag_rh_h`
   - 这说明后续若继续做 `segment static support v3`，优先应保留“耦合 / 滞后 / 晚段响应持续性”这类跨数据集仍能守住的结构特征，而不是重新把大量 dew / ingress 派生量全部堆回主链。
   - 在此基础上，当前又完成了 `segment static support v3` 的首轮保守验证：
     - `v3` 并没有继续放大静态正支持，而是把 `cross-dataset guard` 特征和 `segment memory bank similarity v2` 的三类记忆结果一起接成硬约束。
     - 当前结果是：
       - `old_hard_negative_anomaly_false_positive_count_v3 = 0`
       - `181049` 从 `static_supported_weak_positive` 收敛回 `static_review_weak_positive_guarded`
       - `160246` 收敛为 `static_watch_breathing_hardguard`
       - `20260321 heat-off` 收敛为 `static_watch_confound_hardguard`
     - 这一步的含义不是“静态分支变弱了”，而是当前证据表明：  
       - `181049` 依然有局部正向证据，但还没有通过跨数据集 hard-negative 守门，因此只能继续保留为 `review support`，不能安全升成更强的正参考；  
       - `breathing_watch / confound` 在三类记忆库下继续贴近 `hard_negative`，反而进一步证明当前主线程里保留 `watch` 是正确的。  
     - 因此，`segment static support v3` 当前应理解为“主战场静态支线的守门层”，它的收益是减少误抬和误并训练，而不是直接提高静态覆盖率。
   - 围绕这一步，当前又补齐了 `v3 guarded` 的段级复核链：
     - `segment_review_label_template_v2`
     - `segment_auto_seed_labels_v2`
     - `segment_feedback_loop_v2`
   - 与早期 `v1` 自动种子相比，这一轮最重要的变化是：  
     - `181049` 不再被自动写入 `positive_reference`；  
     - 自动种子当前只确认 `3` 个 `transition_positive`、`1` 个 `breathing_watch`、`1` 个 `confound`；  
     - 回灌后的 `pending` 首位明确变成 `181049` 这一类 `static_review_weak_positive_guarded`，说明当前复核流已经真正切换到“先保守守门，再人工确认”的模式。
   - 在此基础上，当前又把这条链进一步收成了一个可执行工作流：
     - `new_data_review_workflow_v1`
     - `segment_review_labels_working_v2.csv`
     - `review_agenda_v1`
     - `new_data_model_tuning_plan_v1`
   - 这一步的重要意义不是“模型已经被新数据训练得更强”，而是：
     - 新数据已经**明确优化了流程**：从“离线分析 + 人工记忆”升级为“模板生成 -> 保守自动种子 -> 回灌 -> pending 重排 -> 人工 agenda” 的闭环；
     - 新数据**暂时没有直接优化模型本体**：当前还没有证据支持把这批数据直接混进 whole-run `GRU / XGBoost` 重新训练；
     - 真正被优化的是第二阶段主线程的工程可执行性：  
       - `transition` 主分支的正池更清晰；  
       - `181049` 这类 guarded positive 不会再被误并进正参考池；  
       - `160246 / 20260321 heat-off` 这类难例被稳定沉淀进 `breathing / confound` 池；  
       - `transition_secondary_control` 被显式降级为边界验证集，而不是隐式混入主战场。  
   - 因此，对“这批数据进来以后流程有没有优化”的当前正式结论应写成：  
     - **有，而且优化主要体现在流程，不体现在 whole-run 模型精度已经被证明提升。**  
     - 这批数据把当前路线从“研究型分析链”推进成了“可复核、可回灌、可守门”的工程闭环；  
     - 下一步仍应继续围绕 `transition 主导 + static guarded support + watch/confound challenge` 推进，而不是回到统一大模型训练。
   - **当前有效状态（2026-03-27）**  
     - 默认工作入口已经收敛为 `reports/new_data_review_workflow_v1_run1/segment_review_labels_working_v2.csv`；  
     - 默认执行顺序已经收敛为 `3 条 transition_primary_support -> 181049 -> 160246 -> 20260321 heat-off -> 4 条 transition_secondary_control`；  
     - 默认调优动作已经收敛为“先完成人工复核并只重跑 `feedback_loop_v2`”，而不是重启 whole-run `GRU / XGBoost`；  
     - 只有当 `181049` 这类 guarded positive 在人工复核后持续稳定，才考虑调 `guard_score_thresh / weak_support_thresh` 或把它升级到 `supported positive`。
     - 与此同时，当前在线调用的 `seal_v4` 也已经开始做同方向的小步更新：不改外部接口，只在 `ext_high_hum_no_heat` 分支内部补进 `dew point / VPD / level-correlation / lag / response persistence` 这些已验证有效的结构特征，用来压制 `181049` 这类 guarded positive 的误抬，并把 `breathing / heat-off confound` 更稳定地收回 `watch`。
     - 按 `new_data.zip` 的整包对照，`seal_v4-online.2` 相比 `seal_v4-online.1` 在 `20` 个运行里有 `3` 个最终状态发生变化，且都发生在 `ext_high_hum_no_heat` 主战场：  
       - `181049` 从 `static_dynamic_supported_alert` 收回到 `static_hard_case_watch`；  
       - `20260304-120007` 从 `static_dynamic_supported_alert` 收回到 `static_hard_case_watch`；  
       - `20260323-213435` 从 `static_dynamic_supported_alert` 收回到 `static_hard_case_watch`。  
     - 这里要注意：这份对照看的是**最终尾窗状态**，不是整文件扫描过程；对 `20260304-120007` 这类 transition 运行，整文件回放里仍然能看到 `transition_boost_alert` 和多次 `static_dynamic_supported_alert` 标记，因此当前更准确的理解应是：  
       - `seal_v4-online.2` 已经开始把尾窗误抬收得更保守；  
       - 同时它是否伤到 `transition event`，必须继续按 `upload full replay / mqtt 实时流` 口径做事件级核验，而不能只看最终尾窗一帧。  
     - 这一步随后已经补做了 `seal_v4_transition_event_compare_v1` 的整文件事件级对照，结果是：  
       - `7` 个 transition 文件里，`3` 个属于 `transition_primary_mainfield`，`4` 个属于 `transition_secondary_control`；  
       - 主战场 `before_any_transition_hit = 2`、`after_any_transition_hit = 2`，并且 `20260304-120007`、`20260308-172014` 的 `transition_hit_count` 都保持 `2 -> 2`；  
       - `20260323-213435` 这一条在升级前后都没有显式 `transition_boost_alert` 命中，因此它当前属于“尚未新增回归，但也还不是已攻克样本”；  
       - `secondary_control` 维持 `0 -> 0`，没有因为新特征接入而被误打成 transition 正例。  
     - 因而截至 `2026-03-27` 的在线模型结论先更新为：  
       - `dew point / VPD / lag / coupling / persistence` 已经可以保留在在线 `seal_v4` 中；  
       - 当前收益主要体现在**压尾窗误抬、把 guarded positive 更稳定地收回 watch**；  
       - 当时没有证据表明这次升级削弱了主战场 transition 的事件级命中，所以先保留了 `online.2`。  
     - 随后又围绕 `20260323-213435` 做了单样本专项回放，结论更细了一步：  
       - 这条运行不是“没有 transition score”，而是**慢爬升 transition**：在 `2026-03-24 07:00 ~ 13:00` 变更邻域，`delta_in_h` 持续抬升到 `0.55 ~ 0.59`，`delta_ah_in` 达到 `0.08 ~ 0.14`，但长期卡在旧版 `moderate_shape` 的 `delta_in_h >= 0.6` 门槛下方；  
       - 因此在线模型又补了一个更保守的 `sustained_slow_shape`：只有在 `delta_in_h >= 0.55`、`delta_ah_in >= 0.08`、`score >= 0.28`、且连续 `3` 个窗口都满足时，才允许升级成 `transition_boost_alert`；  
       - 这一步已经体现在 `seal_v4-online.3`。  
     - `online.3` 的回归结果是：  
       - `new_data.zip` 上 `transition_primary_mainfield` 从 `2/3` 补到 `3/3`，其中 `20260323-213435` 的 `transition_hit_delta = +11`；  
       - `transition_secondary_control` 仍然保持 `0 -> 0`；  
       - `new_data.zip` 的最终尾窗状态分布不变，说明这次补丁没有把静态尾窗重新放松回去；  
       - 额外对 `old_data/sealed.zip` 与 `old_data/unsealed.zip` 做了真实分支口径检查，没有新增命中。  
     - 因此当前更准确的在线结论应收敛为：  
       - `seal_v4-online.3` 同时保留了 `online.2` 的“尾窗保守 / guarded positive 回收”收益；  
       - 并且把主战场 transition 从原来的 `2/3` 进一步补到了 `3/3`；  
       - 这一步仍然没有改外部接口，因此会同时作用于 `xlsx` 上传链和 MQTT 实时链。
     - 兼容性上，这次在线升级已经天然覆盖当前系统的两条入口：  
       - 实验室 `xlsx` 上传链走的是 `analyze_uploaded_points -> predict_points_window -> call_local_model -> run_local_detection`；  
       - 设备实时 / MQTT 最新点处理走的是 `process_latest_for_device -> call_local_model -> run_local_detection`；  
       - 因此只要前端/后端请求的模型名仍是 `seal_v4`，这次新接入的 `dew / lag / coupling / persistence` 特征会同时作用于上传回放和实时检测，不需要额外改业务逻辑。

7. **`dynamic response probe v1` 已完成首轮验证，但当前定位应是辅助探针**  
   - 在旧数据中，`best_lag_rh_h`、`gain_ratio_dAH_change` 等动态响应特征对 `unsealed vs sealed_strict` 已表现出一定区分能力。  
   - 但在当前分工况小样本中，这些特征方向只部分一致，还不足以替代现有主路线。  
   - 因此这条线当前更适合作为 `interference/watch` 辅助探针，而不是直接升级成新的主判别分支。
   - 进一步把 `外部高湿驱动` 显式拆成 `无热源响应` 和 `冷却响应` 两条分支后，当前结果也更清楚了：  
     - `外部高湿-无热源` 这条物理逻辑并不是“筛不出来”，而是已经能把 `低信号 sealed`、`response-supported unsealed` 和 `response-like sealed hard case` 三种状态分开。  
     - 当前最典型的干扰不是“没有响应”，而是存在 `sealed 但表现出外部驱动样` 的难例，这更像 `材料吸放湿 / 残余湿气 / 结构呼吸效应` 对主信号的污染。  
     - `外部高湿-冷却响应` 当前只在 `unsealed heated` 运行里提取到了明确候选段，但尚缺少足够的 `sealed heated cooling` 对照窗口，因此现在只能作为“理论成立、待后续验证”的显式分支，不能直接升成已验收主能力。
   - 在此基础上进一步验证了 `2h 短窗增强 + 6h 主判定 + 12h 长窗确认` 的谨慎多尺度方案：  
     - 对 `外部高湿-无热源`，当前更合理的落法是 `6h` 做主判定、`12h` 做持续性确认、`2h` 只做局部响应增强；短窗不能直接主导最终结论。  
     - 对 `外部高湿-冷却响应`，`12h` 的累计确认比 `2h` 更有价值；当前短窗没有证明自己能带来额外稳定判别力。  
     - 因此，多尺度 `v2` 当前已经接入 `review_output` 和演示卡片，作为显式补充证据；但它尚未替换全局默认判定链。
   - 围绕主战场 `外部高湿-无热源`，又新增了 `no_heat probe v3`，把这条线进一步收敛成 `早段响应 + 晚段持续性` 的三态解释：  
     - `negative`：早段里外部 RH 虽然上升，但内部 RH 不跟随，属于 `drive-decoupled`。  
     - `supported`：早段外部 RH 上升时，内部 RH 同步跟随；但这种响应没有在后段继续异常持续，因此更像正常的外部驱动耦合。  
     - `breathing_watch`：早段同样会跟随，但后段这种耦合仍然持续，且 `AH 衰减 / 驱动力` 比例接近 0，更像 `材料呼吸 / 释湿` 对信号的污染。  
   - 这一步没有改变当前三态结论，但把 `supported / negative / breathing_watch` 从“多尺度规则结果”进一步落成了更容易解释给老师和工程侧的物理语言。
   - 在此基础上新增了 `cooling residual probe v1`，专门验证“扣除纯降温导致的 RH 理论回升后，冷却段是否还有额外 AH 增益”：  
     - 当前结论不是“冷却段无效”，而是“现有 candidate 更多表现为 `dAH` 相对改善，而不是稳健的外部进湿残差”。  
     - 现有最强的 `unsealed heated` 候选在残差视角下仍然只有 `weak_or_confounded`，并且所选冷却段里 `外部 AH 余量` 为负，这说明当前 `dAH` 变好看并不能直接解释成外部湿气进入。  
     - 因此，冷却段当前仍应保留为 `物理解释探针 + review 辅助证据`，不能提前升成默认主判定分支。
   - 进一步收紧为 `cooling residual probe v2` 之后，结论变得更明确：  
     - `v2` 改成了 `onset 优先 + 外部 AH 余量硬门控`，只有“最早出现且外部确实有正向绝对湿度余量”的冷却块才允许进入后续残差判断。  
     - 在当前数据上，唯一的 `unsealed heated` 冷却候选也被收敛成了 `no_external_headroom`，说明现有冷却块并不满足“外部高湿空气可进入”的物理前提。  
     - 这一步把问题从“冷却段阈值没调好”收敛成了“当前冷却段没有形成可验证的外部进湿条件”；因此这条线后续不该靠调阈值推进，而应等待更清晰的冷却参考段或回到 `无热源高湿` 主战场继续优化。

8. **`evidence fuser v2` 已通过本轮离线验收，但其含义是“运行级决策结构通过”，不是“问题已被统一分类器解决”**  
   - 当前 `v2` 已把三类新状态真正接入运行级输出：`transition_boost_alert`、`static_dynamic_support_alert`、`static_hard_case_watch`。  
   - 它的核心收益不是新增一个黑盒模型，而是把多视角证据变成更稳的工程行为：转移证据增强、静态漏报补回、静态难例主动保守。  
   - 当前离线验收结果显示：`transition_boost_capture_rate = 1.0`，`static_eval_balanced_accuracy = 1.0`，`static_prediction_coverage = 0.667`。  
   - 这意味着当前最适合现场演示的不是“全自动 seal/unseal 分类器”，而是“强证据直接进入 review，难例和干扰主动降级”的决策系统。

9. **`evidence fuser v4` 已完成首轮对照验证，并可作为当前默认路径替换 `v3`**  
   - `v4` 的升级重点仍然只放在数据已经明确支持的部分：保留 `gate / info selector v3` 与 `transition` 多视角分数不变，只把 `no_heat probe v3` 以极小范围接回 `外部高湿-无热源` 静态支线。  
   - 对照结果显示：`status_changed_count = 1`，且唯一变化文件就是 `2026-03-03 181049_unseal_unheated`；它从 `static_dynamic_support_alert` 升级成了 `static_dynamic_supported_alert`，而所有 transition 运行与其他分支都没有回退。  
   - 同时，`v4` 继续保持 `transition_capture_rate = 1.0`、`transition_boost_capture_rate = 1.0`、`static_eval_balanced_accuracy = 1.0`、`static_prediction_coverage = 0.667`，并通过了 “变更仅限 no-heat 主战场” 的保守验收。  
   - 因此当前主路线应正式以 `evidence fuser v4` 作为默认实现，而不是继续停留在 `v3`。

10. **`transition event summary v1` 已完成首轮验证，可直接服务现场演示**  
   - 这一步不引入新模型，只把 `transition_score_v3` 进一步压成 `event_start / event_end / peak_time / duration` 这类事件级输出。  
   - 当前两个 transition 运行都能提取出单一主事件，且 `event_detect_rate = 1.0`、`near_overlap_rate = 1.0`、`peak_in_near_rate = 1.0`。  
   - 这意味着现场展示时，不再需要手工解释一串窗口分数，而可以直接讲“事件从何时开始、何时达到峰值、何时回落”。  
   - 当前演示入口也已同步到这一路径，因此现场默认口径应理解为：`分支感知路由 -> 转移段相对打分 -> 状态段级证据融合 v4 -> transition event summary`。

11. **统一数据接口已落地，可作为后续现场迁移与复核闭环的基线**  
   - 当前已经实现 `Unified Data Interface v1`，并已切换到 `evidence fuser v4` 作为默认来源，把现有主路线统一导出为三张正式表：`run_manifest`、`window_table`、`review_output`。  
   - `run_manifest` 负责描述运行级元数据和当前决策状态；`window_table` 负责沉淀窗口级路由、`segment_id`、`info_score` 和粗粒度标签；`review_output` 负责沉淀待复核队列及其关键证据。  
   - 这一步的价值不是提高实验室指标，而是把当前已验证的决策链收口成可复用的数据接口，便于后续接入现场数据、历史健康窗口库和人工复核结果。  
   - 因此，后续所有迁移与增量实验都应优先基于这三张表继续推进，而不是继续从零拼接不同报告。

12. **`health window bank + similarity ranking v1` 已开始落地，但当前定位必须收紧**  
   - 当前已经基于 `run_manifest / window_table / review_output` 建立了首版健康窗口库和相似性风险排序接口。  
   - 它当前更适合作为“现场迁移骨架”和“复核优先级排序器”，而不是新的实验室主判定器。  
   - 当前健康库只有 `21` 个窗口、`3` 个健康参考运行，规模仍小，因此这条线应理解为：先证明接口、分层和排序流程能跑通，再等待后续现场历史窗口逐步扩库。  
   - 这也解释了为什么当前这条线不以 `seal/unseal` 分类指标为主，而以“能否把新查询相对健康库做偏离排序”作为当前目标。

13. **健康窗口库的复核回灌闭环已经打通**  
   - 当前已经实现 `Health Bank Feedback Loop v1`，可以把 `review_output` 中的人工复核结果回灌成三类动作：  
     - `healthy`：提升为健康参考，增量加入健康窗口库；  
     - `anomaly`：沉淀为异常参考；  
     - `uncertain`：保留在待复核队列。  
   - 首轮闭环验证表明：即使只补少量人工复核结果，也能把健康库从 `3` 个参考运行扩到 `4` 个，并对剩余 `pending` 运行重新做相似性排序。  
   - 这一步的意义不是提高实验室单次指标，而是证明后续现场历史窗口库可以通过“运行级复核 -> 窗口级增量入库”的方式逐步成长。  
   - 因此，后续现场迁移最合理的工程路径已经明确：不是先上更复杂模型，而是先把这条 `review -> bank update -> rerank` 的闭环持续跑起来。

14. **新补充数据已经开始对“健康库 / 相似性支线”产生实质帮助，但帮助的形式需要改写成“段级三类记忆库”**  
   - 仅靠早期的 `health window bank + similarity ranking v1`，这条支线更像“离健康有多远”的单库偏离排序；当 `breathing_watch / confound` 这类 hard negative 出现时，单健康库会天然把它们误解成“离健康远 = 更像异常”。  
   - 新补充数据与回灌后的段级参考池，第一次把三类记忆角色补齐了：
     - `health_core_bank`：主战场 `negative_reference`
     - `anomaly_reference_bank`：`supported positive + transition_positive`
     - `hard_negative_bank`：`breathing_watch + confound`
   - 在此基础上已完成 `segment memory bank similarity v2` 的首轮验证。当前采用的是更适合小样本的 `prototype centroid` 视角，而不是最近邻；核心结果是：
     - `health_core_segments = 2`
     - `anomaly_reference_segments = 4`
     - `hard_negative_segments = 2`
     - `current_mainfield_alignment = 0.75`
     - `tri_memory_auc_anomaly_vs_hardnegative = 0.875`
     - `health_only_auc_anomaly_vs_hardnegative = 0.0`
   - 这说明新数据对相似性支线的真正价值，不是“健康样本数变多了”，而是第一次让相似性支线具备了区分“更像异常参考”还是“更像 hard negative”的结构能力。
   - 但当前结论仍然必须保持克制：
     - `181049` 这类 `supported positive` 仍可能被三类记忆打到 `hard_negative` 一侧；
     - 旧数据里的部分 `strict sealed positive-like` 运行仍会投到 `health_core` 一侧；
     - 因此这条线当前更适合做 `review/rerank` 和现场迁移骨架，不适合替代现有默认主链。
   - 这与上面的 `segment static support v3` 是一致的：  
     - `tri-memory` 当前已经足够承担“保守守门”和 `review/rerank` 的角色；  
     - 但它还不足以安全地把 `weak positive` 直接升级成更强的默认告警。
   - 这也给后续推进方向定了边界：
     - 相似性支线后续应继续朝 `segment memory bank` 方向推进；
     - 旧数据里的 `strict sealed positive-like` 更适合作为 `hard negative controls` 参与外推验证，而不是直接并回训练；
     - 在没有更大规模、按设备维护的历史健康段库之前，这条线不应升级成默认告警分支。

## 5.3 第三阶段：融合层与更复杂模型

只有当同一工况族里至少两个分支表现出明显互补性，才进入更复杂的模型融合层：

- 默认先用 `Logistic Regression` 或 `LightGBM` 做轻量融合。
- `stacking / gating / boosting / MoE` 放到第三阶段，不作为当前首轮主线。
- 如果第一阶段都没有找到高信息工况，第三阶段直接取消，回到实验设计。
- 当前已经实现并采用为默认路径的 `evidence fuser v4` 属于**规则化证据融合 / 决策层**，不属于这里所说的“更复杂模型融合层”。

## 5.4 本轮实验组织方式

所有实验统一按下面这个维度组织：

`工况族 × 窗口大小 × 步长 × 分支模型`

验证方式固定为：

- 按实验运行批次做 `leave-one-run-out`
- 不允许把同一运行批次里的相邻窗口同时放进训练和测试
- 第一轮只输出三类结论：
  - 结论 A：哪些工况应直接排除
  - 结论 B：哪些工况存在稳定可分性
  - 结论 C：状态转移时分数能否在变化区域附近持续抬升

---

## 6. 后续现场迁移

现场迁移不再定义为当前阶段的主要任务，但路线应提前约束好：

1. 先把现场任务定义为**风险排序 / 相似性评分**，不是直接硬分类。
2. 先做**设备内历史健康窗口库**，再做相似性检索与残差偏离。
3. 现场阈值必须依赖小批量人工复核做校准，而不是直接沿用实验室阈值。
4. 若实验室阶段没有建立稳定可分性证据，不应直接推进大规模现场上线。

---

## 7. Go / No-Go 标准

## 7.1 Go 到第二阶段的条件

满足以下条件之一，才进入三分支建模：

- 至少一个高信息工况族在 `leave-one-run-out` 验证下显示出重复性的区分或排序能力。
- 状态转移场景中，异常分数能在多数转移样本的邻域附近持续抬升。

## 7.2 Go 到第三阶段融合的条件

只有同时满足下面两点，才讨论融合层或更复杂模型：

- 至少两个分支在同一工况族内表现出互补性；
- 轻量融合已经显示出比单分支更稳定的收益。

## 7.3 No-Go 条件

若首轮没有任何高信息工况族通过验证，则下一步不是继续换模型，而是：

1. 回到实验设计；
2. 增加更强激励或更清晰的状态切换实验；
3. 重新定义工况边界与标注方式。

---

## 8. 近期执行清单

## P0

1. 先按工况角色把当前实验数据重新归档：低信息、高信息、状态转移、复杂耦合。
2. 建立离线实验输入接口：`run manifest` 与 `window table`。  
   当前已完成 `Unified Data Interface v1`，并同步产出 `review_output` 作为复核闭环入口。
3. 固化首轮验证口径：`leave-one-run-out`、不做总体平均准确率、只看 A/B/C 三类结论。
4. 固化当前演示主路线：`分支感知路由 -> 转移段相对打分 -> 状态段级证据融合 v4 -> transition event summary`。

## P1

5. 判别分支继续保留，但当前不再优先继续调 `XGBoost` 参数；只有在补到更稳定的同工况对照样本后再重启。
6. 第一优先级改为继续优化第一步：把 `工况 gate` 持续推进为 `分支感知路由 + info selector v3`，明确区分 `transition_core`、`transition_context`、`static_threshold_favored`、`static_memory_candidate` 等角色，并优先优化 transition scoring 与 route reason。
7. 历史旧数据作为 `干扰挑战集` 单独维护，不并入主训练集；优先用于验证 `watch / abstain` 逻辑是否稳健。
8. `dynamic response probe` 继续保留，但当前只作为 `interference/watch` 辅助探针验证，不单独升级为主判别分支。
9. 相似性 / 记忆分支虽然已通过支线验证，但与当前静态阈值基线暂未显示出足够的“模型级互补性”，因此当前只进入 `evidence fuser v4`，不进入更复杂轻量融合。
10. 残差分支暂缓，不在第一步路由和干扰处理尚未稳定时继续并行扩线。
11. 只有在“新的第一步分流稳定 + 至少两个静态分支出现互补性”后，才重新开启轻量融合讨论。
12. 只有实验室证据稳定后，再设计现场相似性迁移与人工复核闭环。

---

## 9. 给老师的简短汇报话术

“我们当前确认了一个关键事实：密封与非密封在多数窗口上本来就不可分，因此问题不能再定义成全局统一分类。接下来我们先做实验室可分性验证，先把低信息工况排除，只在高信息工况和状态转移段内比较判别、残差和相似性三类分支。只有当实验室内存在稳定证据后，才进入融合层和现场迁移。” 
